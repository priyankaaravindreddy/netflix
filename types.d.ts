type InputProps = {
  id: string;
  onChange: any;
  value: string;
  label: string;
  type?: string;
};

type MobileMenuProps = {
  visible?: boolean;
};

type AccountMenuProps = {
  visible?: boolean;
};

type NavbarItemProps = {
  label: string;
  active?: boolean;
};
